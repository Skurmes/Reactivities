export interface Activity {
    id: string;
    title: string;
    date: Date; //This might need to change to string.
    description: string;
    category: string;
    city: string;
    venue: string;
}